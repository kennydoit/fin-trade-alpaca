# All dependencies (numpy, pandas, scikit-learn, matplotlib, alpaca-py,
# yfinance, requests, pyyaml, python-dotenv) are provided by the shared
# DependenciesLayer - see aws/layers/dependencies/requirements.txt.
# Do NOT duplicate them here; it blows past Lambda's 250MB unzipped limit.
