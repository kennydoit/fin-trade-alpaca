# Python dependencies for Lambda Layer
# Using flexible version ranges for better compatibility with AWS Lambda
# NOTE: boto3/botocore are NOT included here - the Lambda Python runtime already
# bundles them, and adding our own copy blows past the 250MB unzipped layer limit.
# NOTE: matplotlib is intentionally omitted - predict_short_term.py treats it as
# an optional dependency (diagnostic chart generation is skipped if missing).
# Including matplotlib + its hard deps (Pillow, fontTools) adds ~50MB unzipped,
# which pushes the layer over Lambda's 250MB limit.
alpaca-py>=0.29.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
yfinance>=0.2.28
scikit-learn>=1.3.0,<1.6.0
